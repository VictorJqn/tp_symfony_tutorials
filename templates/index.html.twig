<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Tutorials{% endblock %}</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
    {% block stylesheets %}
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.0.3/dist/tailwind.min.css" rel="stylesheet">
    {% endblock %}
</head>
<body class="font-montserrat text-gray-700 bg-gray-100 min-h-screen flex flex-col">

    <!-- Navbar -->
    {% include 'parts/navbar.html.twig' %}

    <!-- Main Content -->
    <main class="flex-1">
        {% block body %}
        <div class="container mx-auto px-4 py-12">
            <!-- Page Title -->
            <h1 class="text-4xl font-bold text-center text-gray-800 mb-10">
                Sujets Disponibles
            </h1>

            <!-- Subjects Grid -->
            <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6">
                {% for subject in subjects %}
                <a href="{{ path('app_subject_show', {'id': subject.id}) }}" 
                   class="block bg-white p-6 rounded-lg shadow-md hover:shadow-lg hover:bg-gray-50 transition duration-300">
                    <div class="flex flex-col items-center">
                        <div class="w-16 h-16 mb-4 flex items-center justify-center bg-blue-100 text-blue-600 rounded-full shadow-sm">
                            <!-- Icon Placeholder -->
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 20l4-4m0 0l-4-4m4 4H8" />
                            </svg>
                        </div>
                        <h2 class="text-xl font-semibold text-gray-800 text-center">{{ subject.name }}</h2>
                    </div>
                </a>
                {% else %}
                <p class="text-center text-gray-500 col-span-full">
                    Aucun sujet disponible pour le moment.
                </p>
                {% endfor %}
            </div>
        </div>
        {% endblock %}
    </main>

    <!-- Footer -->
    <footer class="bg-gray-800 text-white text-center py-4">
        <p class="text-sm">&copy; {{ "now"|date("Y") }} Tutorials. Tous droits réservés.</p>
    </footer>
</body>
</html>
