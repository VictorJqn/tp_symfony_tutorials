<!DOCTYPE html>

<title>Edit Chapters</title>

{% block body %}

    {{ include('chapters/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('admin_index') }}">back to list</a>

    {{ include('chapters/_delete_form.html.twig') }}
{% endblock %}
